import { DisableUploadDirective } from './app-disable-upload.directive';

describe('AppDisableUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableUploadDirective();
    expect(directive).toBeTruthy();
  });
});
